# Copy this file to .env for local development

# Public URL for admin API (accessible from browser)
PUBLIC_ADMIN_API_URL=/api/admin

# SvelteKit origin
ORIGIN=http://localhost

# Node environment
NODE_ENV=development

# Admin frontend port (internal, accessed via nginx in production)
PORT=3001
